services:
  - type: web
    name: overtime-system
    env: node
    plan: free
    buildCommand: npm ci --omit=dev && npm run init:db
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 10000
    autoDeploy: true
